import { Meta } from '@storybook/blocks';
import { Markdown } from '@storybook/blocks';
import accessibility from '../ACCESSIBILITY.md';

<Meta title="Accessibility (for developers)" />

<Markdown>{accessibility}</Markdown>
